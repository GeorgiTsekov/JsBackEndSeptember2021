{{!-- <main>
    <section id="deatils-page">
        <div class="wrapper">
            <div class="product-img">
                <img src="{{image}}">
            </div>
            <div class="product-info">
                <div class="product-text">
                    <div class="product-text">
                        <h1>Name: {{name}}</h1>
                        <h2>Type: {{type}}</h2>
                        <h4>Year: {{year}}</h4>
                        <h4>City: {{city}}</h4>
                        <p>Description: {{description}}</p>

                        <!-- if there are already tenants of the housing, separate their names with a comma and a space ", "  -->
                        {{#if tenants}}
                        <p>People rented this housing: {{tenants}}</p>
                        {{else}}
                        <!-- If not display: -->
                        <p>People rented this housing: There are no tenants yet.</p>
                        {{/if}}
                    </div>
                </div>

                <!-- if there is no registered user, do not display buttons-->
                <div class="product-btn">
                    <!-- Only for registered user and creator of the housing offer-->
                    {{#if isOwner}}
                    <a href="/housing/{{_id}}/edit" class="edit">Edit</a>
                    <a href="/housing/{{_id}}/delete" class="remove">Delete</a>
                    {{else if user}}
                    <!-- logged in user with available pieces-->
                    {{#if isRented}}
                    <p class="alRentHome">You have already rent this home</p>
                    {{else if isAvailable}}
                    <a href="/housing/{{_id}}/rent" class="rentHome">Rent a home, available {{availablePieces}}
                        housing</a>
                    {{else}}
                    <!-- logged in user with no available pieces-->
                    <p class="no-housing">No Housing Available!</p>
                    {{/if}}
                    {{/if}}
                </div>
            </div>
        </div>
    </section>
</main> --}}

<main>

    <section id="details-page">

        <div class="main_card">
            <div class="card_left">
                <div class="card_datails">

                    <h1>Title: {{title}}</h1>
                    <h3>Created by an author: {{authorFullName}}</h3>
                    <div class="card_animal">
                        <p class="card-keyword">Keyword: {{keyword}}</p>
                        <p class="card-location">Location: {{location}}</p>
                        <p class="card-date">Date: {{date}}</p>
                    </div>

                    <p class="disc">Description: {{description}}</p>

                    <!-- If there is no registered user, do not display buttons-->
                    <div class="social-btn">
                        <!-- Only for registered user and author of the post -->
                        {{#if isOwner}}
                        <a href="/post/{{_id}}/edit" class="edit-btn">Edit</a>
                        <a href="/post/{{_id}}/delete" class="del-btn">Delete</a>
                        {{else if user}}
                        {{#if isntVoted}}
                        <!-- logged in user who has already voted-->
                        <p class="thanks-for-vote">Thanks For Voting</p>
                        {{else}}
                        <!-- logged in users, who have not yet voted-->
                        <a href="/post/{{_id}}/voteUp" class="vote-up">UpVote +1</a>
                        <a href="/post/{{_id}}/VoteDown" class="vote-down">DownVote -1</a>
                        {{/if}}
                        {{/if}}
                    </div>
                </div>
            </div>
            <div class="card_right">
                <img src="{{image}}" alt="image">
            </div>
        </div>

    </section>

    <section id="votes">
        <div class="vote-info">
            <div class="card_left">
                <div class="card_datails">
                    <h1>Votes</h1>
                    <div class="card_vote">
                        <!-- Show the rating of votes, if there are no votes yet, the number must be 0. -->
                        <p class="PV">Total rating of votes: {{ratingOfPost}}</p>
                    </div>
                    <!-- If there are already people who have cast their vote for the post, separate their emails with a comma and a space ", " -->
                    <!-- <p class="disc">People who voted for the post - alex@gmai.com, petar@gmail.com, petq@gmail.com</p>-->

                    <!-- If not display: -->
                    <p class="disc">People who voted for the post -
                        {{#if votedUsers}}
                        {{votedUsers}}
                        {{else}} No one has voted yet.
                        {{/if}}
                    </p>
                </div>
            </div>
        </div>
    </section>

</main>